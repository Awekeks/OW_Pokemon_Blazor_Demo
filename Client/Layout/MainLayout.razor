@using Client.Code.Theme

@inherits LayoutComponentBase

@* Required *@
<MudThemeProvider Theme="@(new OwTheme())" IsDarkMode="true" />
<MudPopoverProvider />

@* Needed for dialogs *@
<MudDialogProvider />

@* Needed for snackbars *@
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        <MudIconButton Icon=@Icons.Material.Filled.Menu Color=Color.Inherit Edge=Edge.Start OnClick=@((e) => DrawerToggle()) />
        OW Blazor Demo App
    </MudAppBar>
    <NavMenu IsDrawerOpen=_drawerOpen />

    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>